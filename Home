<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Krypto Esports | Our Competitors Weakness</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-green: #3BB778;
            --dark-bg: #0a0a0f;
            --card-bg: #13131a;
            --text-gray: #a0a0b0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--dark-bg);
            color: #fff;
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            background: rgba(10, 10, 15, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 255, 65, 0.1);
        }

        .logo {
            font-size: 2rem;
            font-weight: 900;
            letter-spacing: 3px;
            color: var(--primary-green);
            text-shadow: 0 0 20px rgba(0, 255, 65, 0.5);
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { text-shadow: 0 0 20px rgba(59, 183, 120, 0.5); }
            95% { text-shadow: 3px 0 0 #ff00ff, -3px 0 0 #00ffff; }
        }

        nav ul {
            display: flex;
            gap: 3rem;
            list-style: none;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
            position: relative;
        }

        nav a:hover {
            color: var(--primary-green);
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary-green);
            transition: width 0.3s;
        }

        nav a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        #canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            padding: 0 5%;
        }

        .hero h1 {
            font-size: 5rem;
            font-weight: 900;
            letter-spacing: 5px;
            margin-bottom: 1rem;
            line-height: 1.1;
        }

        .hero .krypto-text {
            background: linear-gradient(45deg, var(--primary-green), #00aa2e);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(0 0 30px rgba(0, 255, 65, 0.5));
        }

        .hero .tagline {
            font-size: 1.5rem;
            color: var(--text-gray);
            margin-bottom: 1rem;
            font-weight: 300;
            letter-spacing: 2px;
        }

        .hero .slogan {
            font-size: 2rem;
            color: var(--primary-green);
            font-weight: 700;
            margin-bottom: 3rem;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .cta-buttons {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 1rem 3rem;
            font-size: 1.1rem;
            font-weight: 700;
            border: none;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
            letter-spacing: 1px;
        }

        .btn-primary {
            background: var(--primary-green);
            color: #000;
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.5);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 40px rgba(0, 255, 65, 0.7);
        }

        .btn-secondary {
            background: transparent;
            color: #fff;
            border: 2px solid var(--primary-green);
        }

        .btn-secondary:hover {
            background: var(--primary-green);
            color: #000;
            transform: translateY(-3px);
        }

        /* Stats Section */
        .stats {
            padding: 5rem 5%;
            background: linear-gradient(180deg, var(--dark-bg) 0%, var(--card-bg) 100%);
            position: relative;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .stat-card {
            text-align: center;
            padding: 2rem;
            border: 1px solid rgba(0, 255, 65, 0.2);
            background: rgba(0, 255, 65, 0.05);
            transition: all 0.3s;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary-green);
            box-shadow: 0 10px 40px rgba(0, 255, 65, 0.3);
        }

        .stat-number {
            font-size: 4rem;
            font-weight: 900;
            color: var(--primary-green);
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 1.2rem;
            color: var(--text-gray);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* About Section */
        .about {
            padding: 8rem 5%;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-size: 3rem;
            font-weight: 900;
            text-align: center;
            margin-bottom: 3rem;
            color: var(--primary-green);
            text-transform: uppercase;
            letter-spacing: 4px;
        }

        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .about-text h3 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            color: var(--primary-green);
        }

        .about-text p {
            font-size: 1.1rem;
            line-height: 1.8;
            color: var(--text-gray);
            margin-bottom: 1rem;
        }

        .features {
            display: grid;
            gap: 1.5rem;
        }

        .feature {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1.5rem;
            background: var(--card-bg);
            border-left: 3px solid var(--primary-green);
            transition: all 0.3s;
        }

        .feature:hover {
            transform: translateX(10px);
            background: rgba(0, 255, 65, 0.1);
        }

        .feature-icon {
            font-size: 2rem;
        }

        .feature-text h4 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }

        .feature-text p {
            font-size: 0.9rem;
            color: var(--text-gray);
        }

        /* Content Grid */
        .content-section {
            padding: 8rem 5%;
            background: var(--card-bg);
        }

        .content-grid {
            max-width: 1400px;
            margin: 3rem auto 0;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }

        .content-card {
            position: relative;
            height: 400px;
            background: var(--dark-bg);
            overflow: hidden;
            cursor: pointer;
            border: 1px solid rgba(0, 255, 65, 0.1);
            transition: all 0.3s;
        }

        .content-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary-green);
            box-shadow: 0 20px 60px rgba(0, 255, 65, 0.3);
        }

        .content-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.9) 100%);
            z-index: 1;
        }

        .content-card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 2rem;
            z-index: 2;
        }

        .content-card h3 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: var(--primary-green);
        }

        .content-card p {
            color: var(--text-gray);
            line-height: 1.6;
        }

        /* Footer */
        footer {
            padding: 4rem 5%;
            background: #000;
            border-top: 1px solid rgba(0, 255, 65, 0.2);
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
        }

        .footer-section h4 {
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            color: var(--primary-green);
        }

        .footer-section a {
            color: var(--text-gray);
            text-decoration: none;
            display: block;
            margin-bottom: 0.8rem;
            transition: color 0.3s;
        }

        .footer-section a:hover {
            color: var(--primary-green);
        }

        .social-links {
            display: flex;
            gap: 1rem;
        }

        .social-links a {
            width: 40px;
            height: 40px;
            border: 1px solid var(--primary-green);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }

        .social-links a:hover {
            background: var(--primary-green);
            color: #000;
        }

        .footer-bottom {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(0, 255, 65, 0.1);
            color: var(--text-gray);
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 3rem;
            }

            .hero .slogan {
                font-size: 1.2rem;
            }

            .about-content {
                grid-template-columns: 1fr;
            }

            nav ul {
                display: none;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <div class="logo">KRYPTO</div>
        <ul>
            <li><a href="#home">HOME</a></li>
            <li><a href="#about">ABOUT</a></li>
            <li><a href="#roster">ROSTER</a></li>
            <li><a href="#content">CONTENT</a></li>
            <li><a href="#contact">CONTACT</a></li>
        </ul>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <canvas id="canvas"></canvas>
        <div class="hero-content">
            <h1>
                <span class="krypto-text">KRYPTO</span><br>
                <span style="color: #fff;">IS BACK</span>
            </h1>
            <p class="tagline">THE FASTEST GROWING ORG</p>
            <p class="slogan">Our Competitors Weakness</p>
            <div class="cta-buttons">
                <a href="#" class="btn btn-primary">JOIN THE TEAM</a>
                <a href="#about" class="btn btn-secondary">LEARN MORE</a>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats">
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number">6</div>
                <div class="stat-label">Months of Dominance</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">#1</div>
                <div class="stat-label">Non-Tier 1 Org</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">100%</div>
                <div class="stat-label">Player First</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">∞</div>
                <div class="stat-label">Potential</div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="about">
        <h2 class="section-title">The Legacy Returns</h2>
        <div class="about-content">
            <div class="about-text">
                <h3>Built Different</h3>
                <p>Krypto dominated the Fortnite scene for 6 months straight. We weren't just another org—we were THE destination. Players took pay cuts just to wear our colors. Our content set the standard.</p>
                <p>Now we're back, stronger and smarter. Same energy, same culture, but with the experience and resources to go even harder.</p>
            </div>
            <div class="features">
                <div class="feature">
                    <div class="feature-icon">⚡</div>
                    <div class="feature-text">
                        <h4>Player-First Culture</h4>
                        <p>We built our reputation on treating players right. That's not changing.</p>
                    </div>
                </div>
                <div class="feature">
                    <div class="feature-icon">🎬</div>
                    <div class="feature-text">
                        <h4>Top-Tier Content</h4>
                        <p>Consistent, high-quality content that keeps fans engaged.</p>
                    </div>
                </div>
                <div class="feature">
                    <div class="feature-icon">🏆</div>
                    <div class="feature-text">
                        <h4>Winning Mentality</h4>
                        <p>We attract competitors who want to be the best.</p>
                    </div>
                </div>
                <div class="feature">
                    <div class="feature-icon">🚀</div>
                    <div class="feature-text">
                        <h4>Rapid Growth</h4>
                        <p>From zero to hero in record time. We know how to scale.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Content Section -->
    <section class="content-section" id="content">
        <h2 class="section-title">Latest Updates</h2>
        <div class="content-grid">
            <div class="content-card">
                <div class="content-card-content">
                    <h3>Roster Announcements</h3>
                    <p>Stay tuned as we reveal our championship-caliber roster. Big names incoming.</p>
                </div>
            </div>
            <div class="content-card">
                <div class="content-card-content">
                    <h3>Behind The Scenes</h3>
                    <p>Exclusive content showing what goes into building an elite esports organization.</p>
                </div>
            </div>
            <div class="content-card">
                <div class="content-card-content">
                    <h3>Community Events</h3>
                    <p>Connecting with fans and building the strongest community in esports.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="footer-content">
            <div class="footer-section">
                <h4>KRYPTO ESPORTS</h4>
                <p style="color: var(--text-gray); line-height: 1.6;">
                    The fastest growing esports organization. Built by players, for players.
                </p>
            </div>
            <div class="footer-section">
                <h4>Quick Links</h4>
                <a href="#home">Home</a>
                <a href="#about">About</a>
                <a href="#roster">Roster</a>
                <a href="#content">Content</a>
            </div>
            <div class="footer-section">
                <h4>Contact</h4>
                <a href="mailto:contact@kryptoesports.gg">contact@kryptoesports.gg</a>
                <a href="#">Apply as Player</a>
                <a href="#">Partnership Inquiries</a>
            </div>
            <div class="footer-section">
                <h4>Follow Us</h4>
                <div class="social-links">
                    <a href="#">X</a>
                    <a href="#">IG</a>
                    <a href="#">YT</a>
                    <a href="#">TW</a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2026 Krypto Esports. All rights reserved. | Our Competitors Weakness</p>
        </div>
    </footer>

    <script>
        // Particle animation for hero section
        const canvas = document.getElementById('canvas');
        const ctx = canvas.getContext('2d');

        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        const particles = [];
        const particleCount = 100;

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 1;
                this.speedX = Math.random() * 1 - 0.5;
                this.speedY = Math.random() * 1 - 0.5;
                this.opacity = Math.random() * 0.5 + 0.2;
            }

            update() {
                this.x += this.speedX;
                this.y += this.speedY;

                if (this.x > canvas.width) this.x = 0;
                if (this.x < 0) this.x = canvas.width;
                if (this.y > canvas.height) this.y = 0;
                if (this.y < 0) this.y = canvas.height;
            }

            draw() {
                ctx.fillStyle = `rgba(59, 183, 120, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function init() {
            for (let i = 0; i < particleCount; i++) {
                particles.push(new Particle());
            }
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            // Draw connections
            for (let i = 0; i < particles.length; i++) {
                for (let j = i + 1; j < particles.length; j++) {
                    const dx = particles[i].x - particles[j].x;
                    const dy = particles[i].y - particles[j].y;
                    const distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < 150) {
                        ctx.strokeStyle = `rgba(59, 183, 120, ${0.1 * (1 - distance / 150)})`;
                        ctx.lineWidth = 1;
                        ctx.beginPath();
                        ctx.moveTo(particles[i].x, particles[i].y);
                        ctx.lineTo(particles[j].x, particles[j].y);
                        ctx.stroke();
                    }
                }
            }

            particles.forEach(particle => {
                particle.update();
                particle.draw();
            });

            requestAnimationFrame(animate);
        }

        init();
        animate();

        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
    </script>
</body>
</html>
